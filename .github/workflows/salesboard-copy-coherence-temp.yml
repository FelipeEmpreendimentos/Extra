name: Align SalesBoard marketing copy

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/salesboard-copy-coherence-temp.yml"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          ref: main
      - name: Clarify cancellation copy
        run: |
          python - <<'PY'
          from pathlib import Path
          path = Path('salesboard/index.html')
          text = path.read_text(encoding='utf-8')
          old = '✓ cancele quando quiser'
          new = '✓ cancele a renovação quando quiser'
          if text.count(old) != 1:
              raise SystemExit('Expected cancellation copy was not found exactly once.')
          path.write_text(text.replace(old, new, 1), encoding='utf-8')
          PY
          grep -Fq '✓ cancele a renovação quando quiser' salesboard/index.html
          grep -Fq 'destine parte das suas entradas às metas' salesboard/index.html
          if grep -Fq 'registre aportes' salesboard/index.html; then exit 1; fi
      - name: Commit copy correction
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add salesboard/index.html
          git commit -m "Clarify annual cancellation wording"
          git push origin main
